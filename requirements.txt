# FounderHive helper tools. Install with:  pip install -r requirements.txt
python-docx>=1.1     # markdown -> Word (.docx) delivery
python-pptx>=0.6     # read/update slide decks (optional, for GTM deck work)
openpyxl>=3.1        # read spreadsheets (prospects, expenses, forecast)
